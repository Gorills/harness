diff --git a/ARCHITECTURE.md b/ARCHITECTURE.md
index 136c859..85df8b6 100644
--- a/ARCHITECTURE.md
+++ b/ARCHITECTURE.md
@@ -196,6 +196,7 @@ But the implementation is explicitly workspace-domain based and write-safe:
 - Starting a different Task while the Workspace already has a `working` Task is a conflict; `task_start` must not silently replace it.
 - Read-only calls may derive relevance/display defaults from Workspace + current Task and expose the current Task revision where a subsequent mutation may depend on it.
 - `task_checkpoint` is a mutating operation and therefore MUST include both the intended Harness `task_id` and `expected_revision`.
+- Agent-reported checkpoint verification is bounded semantic evidence (`name`, `passed|failed|not_run`, `evidence`) persisted atomically with the checkpoint; mechanical Git evidence remains Harness-derived.
 - The daemon verifies Task/Workspace ownership and transition validity, then applies an existing-Task mutation only when the stored revision equals `expected_revision`; success increments and returns the new revision.
 - Revision mismatch or a required-but-missing `expected_revision` is a bounded conflict/error with no state/event/knowledge mutation. The caller must refresh/reconcile before retrying; Harness must not silently replay stale semantic content against the newer Task state.
 - A stale call for Task A must never be retargeted to whichever Task is current when the request executes, and a stale writer for Task A must never overwrite a newer checkpoint for Task A.
@@ -243,7 +244,7 @@ Keep server-wide guidance short and front-loaded. Codex documents use of MCP ser
 
 Front-load that operator-facing Task `title`, checkpoint `summary`/`next_step`, and Knowledge title/body are written in Russian for the dashboard. Tool names, field names, and enums stay English. Harness stores the supplied UTF-8 as-is and does not language-detect.
 
-Keep operator-facing chat short: lead with the result; cite code instead of pasting unchanged source or recapping diffs. Hosts decide how naturally they follow server instructions, same as the rest of the bootstrap.
+Keep operator-facing chat short. Checkpoints own durable continuity; chat carries only the human-relevant delta. Lead with the result, do not restate the Task/checkpoint or recap diffs/file lists, and report only material decisions, risks, blockers, and verification unless detail is requested. This is a soft native-host instruction, not a claimed hard output-token limit or model proxy.
 
 ## 9. Local IPC
 
@@ -336,7 +337,7 @@ Stale knowledge is retained as a historical clue, receives ranking penalty, and
 
 ## 14. Skills architecture
 
-Canonical Harness skill registry lives outside repositories. The resolver selects a relevant subset using deterministic project stack, Task hints, and explicit configuration.
+Canonical Harness skill registry lives outside repositories. The resolver selects a relevant subset using deterministic project stack, Task hints, and explicit configuration. Harness ships a compact built-in quality pack into that registry through ownership-aware reconciliation. The built-ins are intent-oriented, composed through the existing `task_hints` mechanism, and validated against supported host surfaces; no second workflow/composition DSL is introduced. Same-id unknown or user-modified canonical content is never overwritten.
 
 Projection is host-native and owned by adapters.
 
diff --git a/README.md b/README.md
index ad613b8..e3967ed 100644
--- a/README.md
+++ b/README.md
@@ -76,10 +76,11 @@ Claude Code registration remains owned through the official `claude mcp` CLI and
 - [ADR-0026: Cursor Workspace identity is project-scoped interpolated root](docs/decisions/0026-cursor-project-scoped-workspace-mcp.md)
 - [ADR-0027: Dashboard binds a fixed loopback port and persists the capability token](docs/decisions/0027-dashboard-fixed-loopback-port.md)
 - [ADR-0028: Hygiene-effective Hidden without host SCM-write enforcement](docs/decisions/0028-hidden-hygiene-effective-policy.md)
+- [ADR-0029: Quality discipline is a compact built-in skill pack plus durable verification](docs/decisions/0029-quality-discipline-verification-and-response-economy.md)
 
 ## Development state
 
-The repository now has a Python 3.13 package, a locked development toolchain, and SQLite schema version 11. Database initialization creates ordered migration metadata, requires WAL mode, enables foreign-key enforcement for Harness connections, probes FTS5 as a runtime capability, and migrates the bootstrap schemas forward without discarding unrelated existing data.
+The repository now has a Python 3.13 package, a locked development toolchain, and SQLite schema version 12. Database initialization creates ordered migration metadata, requires WAL mode, enables foreign-key enforcement for Harness connections, probes FTS5 as a runtime capability, and migrates the bootstrap schemas forward without discarding unrelated existing data.
 
 Schema v11 contains the durable Project/Workspace registry, the rebuildable `indexed_files` Structural Index slice, durable Task/CAS state, one-to-one mechanical Task baseline storage, immutable Task checkpoint/event history, bounded operator feedback attached to immutable Task events, and rebuildable FTS5 candidate indexes for Knowledge and Task semantic retrieval. Task rows remain Workspace-owned, use the minimal `working`/`waiting`/`completed`/`cancelled` states, require an allowed reason only while waiting, and carry a positive monotonic revision. A partial unique SQLite index enforces at most one distinct `working` Task per Workspace even if a caller bypasses normal domain checks. Existing-Task state transitions still require explicit `task_id` plus `expected_revision`; successful transitions increment revision exactly once, stale revisions are non-mutating, and completed/cancelled Tasks are terminal. Human review uses the same contract: Accept completes only a Task waiting for `operator_review`, feedback atomically persists bounded operator text and resumes that same Task, and cancel terminates an explicit non-terminal Task. Pending feedback is surfaced through `project_status` only while it is attached to the current working revision, then clears after the next checkpoint.
 
@@ -87,7 +88,7 @@ The baseline-aware internal creation path now atomically captures the required m
 
 The internal changed-file calculator now compares that durable baseline with a stable current Git/worktree sample without mutating Task state or consulting the Structural Index. Exact unchanged non-opaque pre-existing dirty records are excluded; new dirtiness, changed/cleaned pre-existing dirtiness, conservative opaque entries, and committed tree deltas since the baseline HEAD are included. Task-time rename/copy records contribute both destination and origin paths. The calculation shares a finite deadline across baseline-row streaming, Git identity, dirty-content fingerprinting, committed-tree comparison, and deterministic result merging, and it fails closed if Workspace/Git state changes during the read.
 
-The checkpoint persistence layer applies a checkpoint only to the explicitly named `working` Task using `task_id` plus `expected_revision`. A successful checkpoint increments Task revision exactly once and atomically stores bounded summary/next-step semantics, checkpoint state/wait reason, cumulative mechanical changed paths since Task start, current Git HEAD/branch/dirty-path count, provenance-bearing Knowledge, and an immutable checkpoint timeline event. Stale writers and mechanical calculation failures are fully non-mutating. Public `task_start`/resume and `task_checkpoint` are exposed through bounded daemon IPC and the MCP bridge; verification records and bridge activity history remain later slices.
+The checkpoint persistence layer applies a checkpoint only to the explicitly named `working` Task using `task_id` plus `expected_revision`. A successful checkpoint increments Task revision exactly once and atomically stores bounded summary/next-step semantics, checkpoint state/wait reason, cumulative mechanical changed paths since Task start, current Git HEAD/branch/dirty-path count, provenance-bearing Knowledge, and an immutable checkpoint timeline event. Stale writers and mechanical calculation failures are fully non-mutating. Public `task_start`/resume and `task_checkpoint` are exposed through bounded daemon IPC and the MCP bridge. Checkpoints now persist bounded agent-reported verification records (`name`, `passed|failed|not_run`, `evidence`) atomically with Task state and expose compact status through `project_status` plus bounded evidence through selected Task context; bridge activity history remains a later slice.
 
 New Projects are created with the required `normal` visibility default. The low-level explicit registration primitive remains internal and requires an explicit Project identity plus an inspectable Git worktree; Harness stores the canonical worktree root and Git common directory reported by Git, treats repeat registration as idempotent, fails closed on incompatible root/Project identity, and rejects contradictory visibility policy for Workspaces sharing one Git common directory. Public scan registration reuses an already registered root, attaches a new linked worktree to the one unambiguous existing Project sharing its Git common directory, and creates a new Project for a Git checkout whose common directory is not registered. It does not infer logical Project identity across independent clones or expose visibility-mode transitions.
 
@@ -113,7 +114,7 @@ The global status result remains deliberately small: schema version plus Project
 
 The current internal IPC protocol is independent of MCP wire objects, uses protocol version `1`, allows exactly one request/response per connection, enforces a 16 KiB message limit, uses bounded request/response timeouts, and rejects unknown fields/methods fail-closed. `dashboard_url`, `shutdown`, `runtime_diagnostics`, `workspace_status`, `workspace_task_status`, `workspace_search`, `workspace_index_entry`, `workspace_skills_reconcile`, `skill_cleanup`, `project_search`, `project_context`, `scan_workspace`, `task_start`, and `task_checkpoint` are additive protocol-v1 methods; the original global `status` wire shape remains unchanged. The release upgrader deliberately supports the immediately preceding protocol-v1 daemon that does not know `runtime_diagnostics`: only its structured `invalid_request` response activates a compatibility path that validates legacy `status`, requests the already-supported clean `shutdown`, and then verifies a newly started current-runtime daemon. Other diagnostics failures remain fail-closed. The scan and Task clients use longer bounded receive timeouts for their bounded mechanical work. Canonical POSIX clients lazily start the daemon from the installed Python package when needed, with a three-second readiness bound and existing runtime-directory validation. Windows daemon IPC and canonical Windows transport paths remain unsupported until that separate platform task is proven.
 
-The first skills slice now loads a canonical external registry from `~/.harness/skills`, keeps Harness applicability metadata in `harness.yaml` separate from portable `SKILL.md`, derives deterministic stack evidence from the current Structural Index plus supported manifests, and combines it with durable Task `stack_hints` and explicit resolver include/exclude policy under a bounded visible-skill budget. Projection planning models host compatibility roots so every active host profile sees exactly one Harness copy or the operation fails closed; reconciliation mutates only exact Harness-owned generated skill directories, rejects tracked/user-owned collisions, rechecks target state immediately before mutation, and uses Git's own `rev-parse --git-path info/exclude` path without touching `.gitignore`. `harness scan` supplies the union of current Claude Code and Cursor profiles to that single planner. Partial uninstall recomputes projections for the remaining profiles, while uninstalling the final active supported host removes only Harness-owned generated projections before daemon shutdown. Automated fixtures and installed-wheel acceptance prove the shared projection mechanics; proprietary-host UI visibility remains acceptance-gated.
+The first skills slice now loads a canonical external registry from `~/.harness/skills`, keeps Harness applicability metadata in `harness.yaml` separate from portable `SKILL.md`, derives deterministic stack evidence from the current Structural Index plus supported manifests, and combines it with durable Task `stack_hints` and explicit resolver include/exclude policy under a bounded visible-skill budget. Harness now seeds that registry with a compact 12-skill quality pack covering architecture, testing, backend security, containers, observability, scalability, CI/release, public frontend, complex-change planning, spec audit, independent review, and project conventions. `harness install` and `harness skills sync` reconcile only Harness-owned exact content; same-id user or modified content is never overwritten. `harness skills validate` checks canonical skills against all currently supported host surfaces. Built-in composition deliberately reuses Task `stack_hints`/skill `task_hints` instead of adding a workflow DSL. Projection planning models host compatibility roots so every active host profile sees exactly one Harness copy or the operation fails closed; reconciliation mutates only exact Harness-owned generated skill directories, rejects tracked/user-owned collisions, rechecks target state immediately before mutation, and uses Git's own `rev-parse --git-path info/exclude` path without touching `.gitignore`. `harness scan` supplies the union of current Claude Code and Cursor profiles to that single planner. Partial uninstall recomputes projections for the remaining profiles, while uninstalling the final active supported host removes only Harness-owned generated projections before daemon shutdown. Automated fixtures and installed-wheel acceptance prove the shared projection mechanics; proprietary-host UI visibility remains acceptance-gated.
 
